# Documentation: https://github.com/roots/trellis#wordpress-sites
wordpress_sites:
  onrek.org:
    site_hosts:
      - onrek.wabdesign.fr
    local_path: ../site # path targeting local Bedrock site directory (relative to Ansible root)
    repo: git@github.com:wab/onrek.git
    branch: master
    # subtree: site # Use this if following the roots-example-project structure
    multisite:
      enabled: false
      subdomains: false
    ssl:
      enabled: false
    cache:
      enabled: false
      duration: 30s
    system_cron: true
    env:
      wp_home: http://onrek.wabdesign.fr
      wp_siteurl: http://onrek.wabdesign.fr/wp
      wp_env: staging
      db_name: onrek
      db_user: onrek
      db_password: EthzH4gK
      # Generate your keys here: https://api.wordpress.org/secret-key/1.1/salt/
      auth_key: "generateme"
      auth_salt: "generateme"
      logged_in_key: "generateme"
      logged_in_salt: "generateme"
      nonce_key: "generateme"
      nonce_salt: "generateme"
      secure_auth_key: "generateme"
      secure_auth_salt: "generateme"
